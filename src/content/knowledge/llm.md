明确 prompt 的限制长度
Few‑shot 示例：选择上，选择粒度
多轮对话

数据、测试设计中 prompt 很难稳定的输出；
输出与其期待的所有条件保持一致

Self‑consistency：要持续生成内部多采样，
进行排序，再选出最优答案

思维链适合于思维推理吗？
CoT：大的模型，每一步产生一个后续步骤
ToT：考虑生成多个候选路径

zero‑shot prompt：要求模型去完成任务

元认知提示：
要求模型自我评估需要的信息
让模型思考，自己的思考过程
让模型列出解答的知识来源

上下文窗口：
模型在单次推理能够处理的最大 token 数量

精调各个任务正确，输出有多少是真正正确的。返回平衡所有案例，需要做设计。
当模型输出不确定，自我进行结果差异打分，降低 temperature，减少模型明确的发散。

函数调用在 LLM 中作用：让模型让它转化为调用工具参数，模型本身不返回内容，调用。
约束在 LLM 中，模型生成很低占比的，实际上语言的提示的修改。
中间提示：当模型位于长文档的中间部分时，模型表现往往很差，在此可以使用各类。

one‑shot prompt 投放一个样例的 few‑shot prompt
JSON Mode 在指令调优当中主要用途：强制模型输出符合 JSON 格式的数据。

减少模型幻觉：要求模型引用来源，实在不确用明确：不知道。

在评估 prompt 效果时，LLM‑as‑judge 方法：使用另一个 LLM 对输出进行打分评估。

链式截断：多轮调用模型，可以基于上一次输出进行修改。

Agent：将模型的思考结果在提供的算子模板上，成为能控推理。

宪法 AI：定义一组指导模型行为的原则 / 规则，模型用这些规则自我批评和修正输出。

提升模型对长文档理解：
真实摘要，MyRAG 实战
看处理短文本

token 优化的目标：
保证足够的前后文，减少窗口的 token 消耗，降低成本

RLHF：基于人类训练的连续向量，通过标度优化得到。
ReAct 框架结合了推理跟行动。

多轮对话时，注意上下文窗口限制，历史对话截断，维持每一轮对话。
° 用术语和新约束来进行 prompt 设计
2° Step‑back prompt：引导模型思考问题然后后退一步，变更更高层次的概念，再把这些原理应用
3° prompt chain：将复杂任务拆成多个子任务，每个子任务输出作为下一任务输入
4° 思维做图、下位及多数，让模型执行特定范式的专业任务，拆解工程
5° prompt 压缩：自动精简 prompt 内容，不损失语义前提下减少 token 消耗
6° 先提问再回答，让模型生成答案前明确问题，避免答非所问
7° prompt 回归测试：修改 prompt 后回归，保证修改不会降低原有能力，通过断言
8° 通过 BedOne 后的知识，过滤旧的，接受新的
AI 在 prompt 中去控制模型，重新编排上下文
9° Persona 人格：普通角色执行；Role 全面的身份设计，角色描写细节，指定职业专业

10° 对给定的模型做优化，写 prompt 要点
11° A/B 测试时，只改变一个要点，以便准确的回归，看效果差异
12° lets think step by step 用 zero‑shot
chain‑of‑thought

13° few‑shot prompt：在提示中提供足够多案例，帮助模型理解任务格式

14° 对抗提示手段：主动改写，让模型产生有害输出或者绕过安全风险

15° System Prompt 战斗手段：约束模型的行为边界、语言和能力范围。

16° Instruction Follow：解析文本基础上，模型输出，处理了模板中的 JSON、行转条件等填空格式。

上下文学习：仅仅 prompt 好的示例，在推理阶段，学会完成新任务。

分解式提示是一种 prompt chaining 的设计方法，通过若干提示处理不同子任务，
复用提示、拆分，图简单任务用于提示文本不希望出现的内容。

Prompt 海战：模型在接收到信息最初的 system prompt 约束
chain‑of‑Thought：通过引导模型展开中间推理线索，提升推理的准确性。

Role prompt：在提示开头指定角色，扮演特定角色（比如做一位咨询讲师），
一旦上述的推理验证，再输出最终结果。

Self‑Consistency：多次采样同一个问题的输出，其结果中通常正确那一个（概率 0‑1）都可以控制输出的结构，确保关键内容不被截断。

Temperature 和 Top‑P：控制输出 token 占比，确定生成时的选择。

Token 注释与边界：输入大量的，接近达到用户窗口时，选择大幅下降。

prompt 过后后在测试集上来表现很好，仅仅是过拟合，用于仔细梳理每道测试。

截词器：让模型的回忆生成与查询分开，不用内存，语义关注。

思维链的提示：让模型生成可执行的代码可以问，不用内存，语义关注。

RAG 与 Prompt 工程结合：将检索到的相关数据插入 prompt 上下文，给模型提供真实的，减少模型幻觉；要求模型引用来源，成立不确定的明确，保持不幻想。

真实验证：需要做验证，考察模型的准确性。

在 prompt 明确要求模型标注来源并引用，帮助模型在回答时区分已知和未知。
免混淆：清晰区分拉手的不同部分，防止内容混淆和注入。

Output‑scaffolding 在 prompt 中提供输出的高阶结构 / 格式，引导模型按格式完成。
