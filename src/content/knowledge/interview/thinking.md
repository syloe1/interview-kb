越是vibe coding，越要重视底层原理的理解
介绍项目多讲 “为什么” 和踩过的坑。

计网卡、OS 要学明白，
讲简历：做了什么 → 为什么 → 踩了什么坑

抽象层设计是 Agentic AI 时代的重要能力

块设备实现：`struct block_device_operations`
`struct request_queue`

剩下的 read/write/mmap，都是文件系统的架子

直接让进程访问文件系统，
不就全乱套了吗？

抽象层：文件系统
硬链接，允许一个文件被多个目录引用
产品需求文档 PRD
技术规格文档 spec
系统架构
技术选型
数据模型
内存结构
API 接口

claude.md 工程说明文档
hooks 事件钩子，特定时机自动执行
skills 专业知识包，内置能力扩充
LSP：代码导航
MCP：连接外部工具和数据源
子 agent：独立上下文并行对话

setting.json：可以允许一些命令，禁止一些命令

自己设计
-> 卡壳 → review -> 设计

先了解一些 → 自己设计，不再再去查
做工作能反复迭代的项目

设计→扩展，定时器最小堆 vs 时间轮， 比较性能
压测，找瓶颈，重构
要有一个能拿得出的项目，自己设计、不断迭代。

##

读别人代码看到别人设计的

为具体问题而服务，要明白它为什么这样设计？

失自己画架构，明白各个模块的作用？
写工程日志，长时间去看到自己设计也能一步步成型。
